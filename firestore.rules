rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    
    // Allow read & write globally to support fluid client-side offline/online hybrid simulation with mock accounts.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
